const State = () => {
    let count = 0;

    return (
        <>
            <button>-</button>
            <div>{count}</div>
            <button>+</button>
        </>
    );
}

export default State;